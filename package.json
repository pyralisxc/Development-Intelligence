{
  "name": "development-intelligence",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Project-neutral MCP tooling for codebase structure and cross-representation technical parity.",
  "engines": { "node": ">=22" },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test dist/tests/*.test.js",
    "verify": "npm run build && node --test dist/tests/*.test.js && node scripts/check-generic-boundary.mjs",
    "start": "node dist/src/http.js",
    "stdio": "node dist/src/stdio.js"
  },
  "dependencies": {
    "typescript": "5.8.3"
  }
}
